import 'package:flutter/material.dart';

class MovieAddition extends StatelessWidget {
  const MovieAddition({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(),
        body: const Center(
          child: Text('映画登録フォームをここに書く'),
        ));
  }
}
